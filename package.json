{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "npx pnpm run typecheck && npx pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "npx pnpm run typecheck:libs && npx pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "dev:api": "PORT=5001 npx pnpm --filter @workspace/api-server dev",
    "dev:sandbox": "PORT=8081 BASE_PATH=/ npx pnpm --filter @workspace/mockup-sandbox dev",
    "dev:portfolio": "PORT=8080 BASE_PATH=/ npx pnpm --filter @workspace/portfolio dev",
    "dev": "npx pnpm run dev:api & npx pnpm run dev:sandbox & npx pnpm run dev:portfolio"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
